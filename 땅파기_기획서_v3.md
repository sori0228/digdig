# 땅파기 — 기획서 v3 (프로토타입 완성판)

작성일 2026-09-14 · 범위: `Main.unity` 프로토타입 구현 현황 정리
구조: 시스템(어떻게 작동하는가) - 목적(왜 있는가) - 컨텐츠(구체적 수치/데이터)
`땅파기_구현지시서_1.md`의 1~11단계(1차 핵심 감각 + 2차 웹과 귀환 + 3차 컨텐츠)까지 구현 완료.

---

## 0. 한 줄 정의

> **어두운 땅속을, 파는 만큼 줄어드는 램프 기름에 의지해 내려가다가,
> 전리품을 들고 지상으로 돌아오면 이기는 게임.**

기름은 시간이 아니라 **판 횟수**로 줄어든다. 그래서 매 클릭이 "이걸 팔 가치가 있나"라는 질문이 된다.
동시에 지고 갈 수 있는 전리품은 6칸뿐이라, 내려갈수록 "한 층 더 갈까, 지금 돌아갈까"라는 두 번째 질문이 겹쳐진다.

---

## 1. 전체 루프

1. 지상에서 시작, 기름 90을 들고 판다
2. 파는 만큼(1회=1턴) 기름이 줄고, 기름 비율만큼 램프 반경(시야)이 좁아진다
3. 연료 광물을 캐서 기름을 회복하거나, 전리품 광물을 인벤토리(6칸)에 채운다
4. 목표 깊이(20m)에 닿으면 "이제부터는 자유" 알림 — 더 내려가도 되고 바로 돌아가도 된다
5. 지상으로 복귀하면 **클리어** (전리품 목록 + 총 가치 표시)
6. 기름이 0이 되면 **게임오버** — 인벤토리 전부 상실

---

## 2. 시스템별 정리

### 2.1 이동 & 벽 매달리기

- **시스템**: Rigidbody2D 기반 실시간 플랫포머(A/D 이동, Space 점프). 벽에 붙으면(`WallCling`) 중력 0으로 고정되고, Space로 반대 방향으로 튕겨 나온다. 매달린 동안은 일반 이동 로직이 꺼진다.
- **목적**: 조작 자체가 기분 좋아야 그 위에 얹는 모든 시스템이 의미가 생긴다(지시서 1단계 원칙). 벽 매달리기는 웹 발판과 묶여 수직 이동의 자유도를 만든다.
- **컨텐츠**: 이동속도 6, 점프높이 2.5칸, 벽 이탈 점프 속도 (5, 8)

### 2.2 파기

- **시스템**: 실제 지형은 `GridWorld`의 2차원 배열이 갖고 Tilemap은 표시 전용. 좌클릭을 누르고 있으면 마우스 방향의 인접 8칸 중 한 칸을 겨냥해 원형 게이지가 차오르고, 다 차면 파괴된다. 마우스를 떼거나 칸을 바꾸면 진행도 초기화. 모래는 파괴되면 위 모래가 연쇄로 떨어진다.
- **목적**: "파는 손맛"의 즉각적 피드백. 블록마다 걸리는 시간이 달라야 재료 자체에 성격이 생긴다.
- **컨텐츠**: 흙 0.3초 / 모래 0.2초(연쇄) / 암반 1.2초 (모두 `BlockType` 에셋으로 인스펙터에서 조정 가능)

### 2.3 램프 시야 (안개)

- **시스템**: Global Light 2D를 0.05로 어둡게 깔고, 플레이어의 Point Light 2D(램프)만 밝다. 한 번이라도 빛이 닿은 칸은 "탐사 기록"으로 남아 20% 밝기로 계속 보인다(별도 오버레이 Tilemap).
- **목적**: 이 게임의 정체성. 시야가 곧 정보이고, 시야가 좁아지는 것 자체가 압박이 된다.
- **컨텐츠**: 반경 1.5(기름 0%) ~ 7(기름 100%), 탐사 기록 밝기 20%

### 2.4 기름 (턴제 자원)

- **시스템**: 실시간으로 줄지 않는다. **파기에 성공할 때마다(1회=1턴) 정확히 1씩 감소**하고, 웹 발판 설치 같은 직접 행동은 별도 고정 비용이 즉시 빠진다. 램프 반경은 매 프레임 현재 기름 비율로 보간된다. 0이 되면 즉시 게임오버.
- **목적**: 처음엔 "초당 감소"였으나, 이러면 오래 고민하는 것 자체가 손해가 되어 신중한 플레이를 벌주는 구조였다. 파기라는 **행동**에 비용을 매기면, 고민하는 시간은 공짜이고 실제로 판 것만 대가를 치른다 — 원래 기획서 v1의 "모든 클릭이 되돌릴 수 없는 결정" 정신과 더 가깝다.
- **컨텐츠**: 시작 90 / 파기당 -1 / 웹 설치 성공 -8, 실패 -4 / 가스 -20 / 연료 광물로 회복(석탄 +5, 기름주머니 +30)

### 2.5 광물 & 광맥

- **시스템**: 광물은 무작위 점이 아니라 **줄기(vein)**로 생성된다 — 시작점 하나에서 방향을 정해 3~6칸 이어지고, 35% 확률로 방향이 꺾인다. 캐면 연료(즉시 기름 회복)와 전리품(인벤토리 저장)으로 나뉜다. 램프 반경 안에 있는 미채굴 광물은 반짝인다. 광물·가스·빈칸 중 무엇이든 인접한 칸은 전부 **똑같은 색**으로 물든다(표면 징후) — 좋은 것만 예고하면 단서가 안내판이 되어버리기 때문.
- **목적**: "추론 → 확인 → 보상" 루프. 하나를 찾으면 "이 방향으로 더 있겠다"는 생각이 들어야 탐색이 도박이 아니라 실력이 된다.
- **컨텐츠**:

  | 광물 | 분류 | 값 | 최소 깊이 |
  |---|---|---|---|
  | 석탄 | 연료 | +5 | 1층(1~10m) |
  | 철 | 전리품 | 10 | 2층(11~20m) |
  | 기름주머니 | 연료 | +30 | 2층(11~20m) |
  | 보석 | 전리품 | 30 | 3층(21~30m) |
  | 큰보석 | 전리품 | 80 | 4층(31~40m) |

  광맥 개수: 광물종류당 6개(기름주머니·큰보석은 3개), 길이 3~6칸

### 2.6 층별 성질

- **시스템**: 깊이 10m 단위로 4개 층이 고정되어 있고, 층마다 암반/모래 비율과 가스 유무, 배경색이 다르다. 층 경계를 넘으면 화면에 층 이름이 잠깐 뜨고, 카메라 배경색이 부드럽게 전환된다.
- **목적**: 반복 플레이의 학습 동력. "이 깊이는 암반이 많으니 폭탄이 필요하겠다", "여기서부턴 가스를 조심해야지" 같은 지식이 쌓여야 랜덤 생성이어도 질리지 않는다.
- **컨텐츠**:

  | 층 | 깊이 | 암반 확률 | 모래 확률 | 가스 |
  |---|---|---|---|---|
  | 1층 표토 | 1~10m | 5% | 2% | 없음 |
  | 2층 암반대 | 11~20m | 45% | 5% | 있음 |
  | 3층 수맥대 | 21~30m | 20% | 35% | 없음 |
  | 4층 심부 | 31~40m | 65% | 10% | 있음 |

### 2.7 가스 주머니

- **시스템**: 2층·4층에 배치. 파면 기름 -20과 함께 주변 3×3이 즉시 파괴된다(이 파괴는 턴을 소모하지 않는 "조용한" 파괴). 화면이 짧게 흔들린다. 표면 징후는 광물과 동일하게 표시되어 미리 구분할 수 없다.
- **목적**: 손해이면서 동시에 길을 뚫어주는 양면적 발견. "일부러 터뜨릴까?"라는 진짜 도박이 생긴다. 나쁘기만 한 함정보다 훨씬 재미있다.
- **컨텐츠**: 발생 확률 2% (해당 층의 빈 칸 중), 피해 -20, 파괴 범위 3×3

### 2.8 웹 발판

- **시스템**: 벽에 매달린 상태에서만 우클릭으로 조준선이 뜬다. 최대 6칸까지 뻗고 벽에 막히면 그 앞칸에서 멈춘다. 뗄 때 벽을 찾았으면 그 자리에 단방향 발판(위에서 올라설 수 있고 아래에서 점프하면 통과) 생성 + 기름 -8, 6칸 내내 벽이 없었으면 실패 + 기름 -4만 소모. 동시에 5개까지만 존재하고 6번째를 놓으면 가장 오래된 게 사라지며, 다음에 사라질 웹은 반투명하게 미리 보인다. 아래+Space로 발판을 통과해 내려갈 수 있다.
- **목적**: 파낸 구멍 사이를 수직으로 이동할 자유도를 준다. 5개 제한 때문에 "어디에 쓸지" 계획하게 된다.
- **컨텐츠**: 설치 비용 8 / 실패 비용 4, 동시 개수 5, 최대 길이 6칸

### 2.9 인벤토리 & 귀환

- **시스템**: 전리품(철/보석/큰보석) 전용 6칸 인벤토리. 가득 찬 상태에서 새 전리품을 캐면 게임이 멈추고 "무엇을 버릴까" 선택 UI가 뜬다(새로 캔 것을 포기하는 선택지도 있음). 목표 깊이(20m)에 처음 도달하면 알림이 뜨고 그 뒤로는 자유. 지상(깊이 0 근처)으로 돌아오면 클리어 — 전리품 목록과 총 가치를 보여준다. 기름이 바닥나면 게임오버 화면에 "잃어버린 것" 목록을 보여준 뒤 인벤토리를 전부 비운다.
- **목적**: 로그라이트 특유의 위험 감수 판단. "한 층 더 갈까, 지금 돌아갈까"가 실제로 떠올라야 이 시스템이 성공한 것이다.
- **컨텐츠**: 인벤토리 6칸, 목표 깊이 20m

---

## 3. 이번 프로토타입에서 제외된 것

| 항목 | 사유 |
|---|---|
| 지하수 (물속 이동 2배 느림) | 구현지시서 12단계 "선택 사항", 우선순위 낮음 |
| 낙하 경직 (5칸 이상 낙하 시 경직+기름 손실) | GameConfig에 수치는 이미 있으나(`fallStunThreshold` 등) 로직 미구현 |
| 스테이지 2·3 (목표 깊이 30m·40m) | 현재는 스테이지 1(목표 20m)만 존재 |
| 효과음 · 배경음 | 오디오 에셋 파이프라인 미구축 |

---

## 4. 알려진 튜닝 포인트 (플레이해보고 조정할 것)

- **표면 징후가 시각적으로 너무 넓게 퍼짐**: 초기 테스트에서 광맥·가스 밀도가 높아 인접 칸 대부분이 물드는 현상 확인. `GridWorld.veinsPerOre`(현재 6) 또는 `gasChance`(현재 2%)를 낮추는 것을 고려
- 그 외 모든 수치는 `GameConfig`, `BlockType_*`, `OreType_*` ScriptableObject와 `GridWorld` 인스펙터에서 코드를 건드리지 않고 바로 조정 가능

---

## 5. 플레이테스트 체크리스트

구현지시서가 요구한 판정 기준을 그대로 가져온다. 직접 플레이해서 확인할 것.

- [ ] 움직이는 것 자체가 기분 좋은가 (1단계)
- [ ] 파는 손맛이 있는가, 암반이 확실히 오래 걸리는가 (2단계)
- [ ] 램프가 어두워질 때 초조한가 ← **1차 핵심 판정**
- [ ] 기름을 채우려고 일부러 옆길로 새게 되는가 (5단계)
- [ ] 웹으로 올라가는 게 실제로 가능한가, 5개를 어디 쓸지 계획하게 되는가 (7단계)
- [ ] **"한 층 더 갈까, 지금 돌아갈까"를 실제로 고민하는가** ← **2차 핵심 판정**
- [ ] 광물 하나를 찾으면 "이 방향으로 더 있겠다"는 생각이 드는가 (9단계)
- [ ] 내려간 느낌이 나는가, 어느 깊이인지 감으로 알 수 있는가 (10단계)
- [ ] 파는 게 무서워졌는가, 금 간 표시 앞에서 망설이게 되는가 (11단계)
