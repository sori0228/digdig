# 땅파기 — 기획서 v2 (구현 현황 반영)

작성일 2026-09-14 · 범위: v1 기획서를 실제 구현에 맞춰 갱신
v1과의 관계: 기본 정신(기름 하나로 자원 통합, 시작 기름을 빠듯하게 줘서 채굴을 생존 조건으로 만든다)은 그대로 유지하되, 조작 방식과 이번 라운드에 넣지 않은 항목을 실제 코드 기준으로 다시 정리한 문서.

---

## 1. 개요

### 한 문장 정의

> **보이지 않는 땅속을 램프 기름이 떨어지기 전에 파 내려가는 게임.
> 기름은 오직 땅속에서 발견하는 것으로만 채울 수 있다.**

v1과 동일한 긴장감을 목표로 하되, **조작은 턴제가 아니라 실시간**이다 (아래 2.2 참고). "내려가라"는 압력과 "옆으로 파라"는 압력이 충돌하는 것은 여전히 이 게임의 핵심이다.

### 기본 사양 (구현 기준)

| 항목 | v1 기획 | v2 실제 구현 |
|---|---|---|
| 시점 | 2D 사이드뷰, 타일 그리드 | 동일 |
| 조작 | 마우스 클릭으로 팔 칸 선택 (캐릭터 이동 조작 아님) | **WASD 이동 + 스페이스 점프(실시간 물리) + 마우스 클릭으로 발굴.** 캐릭터 이동 자체는 자유. 클릭으로 발굴하면 그 자리에서 즉시 타일이 사라짐 |
| 진행 | 턴제 — 클릭할 때만 세계가 변함 | **실시간.** 중력은 Rigidbody2D 물리로 상시 작동 (아래가 비면 자동 낙하) |
| 맵 크기 | 폭 12 × 깊이 40 | 동일 (구현 완료) |
| 승리 | 최하단 도달 | 동일 |
| 패배 | 램프 기름 0 | 동일 |

> **왜 턴제를 채택하지 않았는가**
> v1은 "한 번 내려가면 되돌릴 수 없다"는 긴장을 턴제로 강제하려 했지만, 이번 라운드에서는 기존 프로토타입의 실시간 플랫포머 조작감을 그대로 살리기로 결정했다. 중력에 의한 자동 낙하와 "위로 못 올라간다"는 원칙은 유지되지만, 이는 물리 낙하 + 파낸 자리가 다시 채워지지 않는다는 규칙으로 자연스럽게 구현된다.

---

## 2. 기본 규칙

### 2.1 그리드

- 폭 12칸 × 깊이 40칸 (`TerrainGenerator.width/depth`)
- 10칸 단위로 4개 층 — 아래 5장 참고
- 맨 위 2줄은 항상 밝음 (안개 없음)
- 최하단 도달 시 클리어

### 2.2 이동과 중력

- WASD로 좌우 이동, 스페이스로 점프 (`PlayerMover`)
- 플레이어 아래가 빈칸이면 Rigidbody2D 중력으로 자동 낙하 — 낙하는 기름을 소모하지 않는다
- 파낸 자리는 다시 채워지지 않으므로(모래 제외) 한 번 뚫은 길은 사실상 돌아갈 수 없는 통로가 된다 — v1의 "되돌릴 수 없는 결정" 정신은 유지

### 2.3 블록

| 블록 | 성질 | 필요 히트 수 (곡괭이/드릴) | 폭탄 |
|---|---|---|---|
| 흙 | 기본 | 1 | 1 |
| 암반 | 단단함 | 3 | 1 (항상 즉시 파괴) |
| 모래 | 제거하면 위 모래가 연쇄 낙하 | 1 | 1 |
| 상점 벽 | 사실상 못 뚫음 | 20 | 1 (파괴는 되지만 사실상 안 씀) |
| 빈칸 | 낙하 발생, 인접 칸 안개를 걷어냄 | — | — |

> v1의 "경도 대비 도구 성능 비교" 공식(초과 하드니스 × 배율)은 폐지하고, **블록 종류별 고정 히트 수**로 단순화했다. 폭탄은 하드니스와 무관하게 항상 1회에 파괴된다 — "기름 4를 내고 시야/길을 사는 도구"라는 v1의 의도를 그대로 살린 것.

### 2.4 도구 — 기름 하나로 통합 (v1 그대로 구현)

| 도구 | 범위 | 기름 비용 |
|---|---|---|
| 곡괭이 | 1칸 | 1 |
| 드릴 | 직선 3칸 (플레이어 기준 클릭 방향) | 2 |
| 폭탄 | 3×3 | 4 |

도구별 개별 횟수/영구 업그레이드는 완전히 삭제했다. 모든 비용은 기름 하나로만 나간다.

---

## 3. 핵심 자원 — 램프 기름

- 화면 좌상단에 **막대 게이지**로 상시 표시 (`DigController.DrawOilBar`)
- 도구 사용마다 고정 비용만큼 감소, 0이 되면 즉시 실패 (`OnOilDepleted` 이벤트 → `GameCycleManager`가 게임오버 처리)
- **모래에 깔리면(끼이면) 초당 기름이 감소한다** — v1엔 없던 규칙. 원래 체력으로 처리하던 위험을 기름으로 통합하며 자연스럽게 흡수됨

### 시작 기름의 결정 방식 (v1 공식 그대로 구현)

```
시작 기름 = GetStraightLineOilCost(플레이어 시작 x열) × startOilMultiplier(기본 0.75)
```

`GetStraightLineOilCost`는 표면부터 바닥까지 그 x열을 곡괭이로만 뚫는 데 필요한 히트 수 총합이다. 즉 **직진만 해서는 절대 바닥에 닿을 수 없다.**

> **v1과의 차이**: v1은 기름을 광물로 회복하는 것을 전제로 이 공식을 설계했다. 이번 라운드는 광물이 아직 없어서(8장 참고) 회복 수단이 보물 발견과 상점뿐이다. 따라서 현재 빌드는 "직진 불가능"은 맞지만 "우회해도 항상 클리어 가능"까지는 보장되지 않는다 — 광물이 들어가면 v1의 원래 밸런스 의도가 완성된다.

---

## 4. 탐험 — 안개 (단순화된 1차 구현)

### 구현된 것

- 별도 `FogLayer` 타일맵이 Ground보다 위에 렌더링되며, 안 밝혀진 칸을 불투명하게 덮는다
- 밝혀지는 조건 (`TerrainGenerator.RevealAround`, `InitFog`):
  - 표면 2줄은 생성 시점부터 항상 밝음
  - 상점 내부 같은 빈 칸과 그 인접 8칸은 생성 시점부터 밝음 (동굴 자연 시야 규칙을 선반영)
  - 타일을 파괴하면 그 칸 + 인접 8칸이 밝아짐
  - 모래가 무너져 내리며 새로 빈 칸이 생기면 그 칸도 같은 방식으로 밝아짐

### v1과의 의도적 차이

> **v1은 "안개가 내용물만 가리고 지형 윤곽(흙/암반/모래 구분)은 가리지 않는다"고 명시했다.** 지금 구현은 이 구분이 아직 없다 — 안개가 걷히기 전에는 흙인지 암반인지조차 안 보이고 완전히 캄캄하다.
>
> 이렇게 단순화한 이유: 지금은 캐도 나오는 게 없어서(광물 없음) "내용물"이라는 개념 자체가 존재하지 않는다. 광물/가스를 넣는 다음 단계에서 "지형은 미리 보여주고 내용물만 숨기는" 2단계 안개로 다듬는 것이 v1 4.2절의 의도를 정확히 구현하는 길이다.

### 아직 없는 것 (v1 4.3절 단서 4종 중 미구현)

- 인접 신호 (주변 8칸에 특별한 것이 있으면 점 표시)
- 표면 징후 (물기 배어나오는 흙, 금 간 암반 등)
- 줄기형 광맥 생성
- 이 세 가지는 전부 "밝힐 내용물"이 있어야 의미가 생기므로 광물/가스 도입과 함께 구현하는 게 맞다.

---

## 5. 층별 성질 (v1 그대로 구현, 광물 칸만 비어있음)

| 층 | 깊이 | 암반 확률 | 모래 청크 배율 | 비고 |
|---|---|---|---|---|
| 1 표토 | 1~10 | 5% | ×0.5 | 흙 위주, 배경 갈색 톤 |
| 2 암반대 | 11~20 | 45% | ×0.5 | 암반 섞임, 배경 회색 톤 |
| 3 수맥대 | 21~30 | 20% | ×2.5 | 모래 많음, 배경 청록 톤 |
| 4 심부 | 31~40 | 65% | ×0.3 | 암반 많음, 배경 검은 톤 |

- 카메라 배경색이 플레이어 깊이에 따라 층 색으로 서서히 전환된다 (`CameraFollow`) — v1 7절 6단계("배경색 변화")를 미리 반영
- 광물/위험(가스·지하수)은 아직 층별로 배치되지 않음 (8장 참고)

---

## 6. 보물과 상인 (v1 8절 "이번 범위 제외" 항목이었으나, 기존 프로토타입 자산을 살려 유지)

v1은 유물/보물을 이번 범위에서 제외하도록 권고했지만, 기존 프로토타입에 이미 구현되어 있던 자산(보물 3×3, 상인 NPC)을 완전히 버리기보다 **기름 경제에 맞춰 재해석해 유지**하기로 했다.

- **보물**: 지표면 근처에 3×3 영역이 항상 하나 배치된다. 첫 타일을 파내면 "무언가 묻혀 있다" 안내가 뜨고, 전부 파내면 **기름 +15 회복** (v1처럼 무작위 유물을 주던 방식은 삭제)
- **상인**: 상점 생성 시(2~3층 사이 벽으로 둘러싸인 방) 안에 등장. 클릭하면 상점 UI가 열리고, **기름을 내고 "보물 위치 표시" 서비스**를 살 수 있다 (기본 8기름). 예전에 있던 "도구 충전 회복" 판매는 삭제 — 기름을 기름으로 되사는 것은 자기모순이라 의미가 없어짐
- 재화(currency)와 재화 기반 영구 업그레이드 페이즈는 완전히 삭제

---

## 7. 수치 초안 (실제 인스펙터 기본값)

| 항목 | 값 |
|---|---|
| 맵 크기 | 폭 12 × 깊이 40 |
| 층 간격 | 10칸 |
| 시작 기름 배율 | 직진 최소 비용 × 0.75 |
| 곡괭이 / 드릴 / 폭탄 비용 | 1 / 2 / 4 기름 |
| 흙·모래 / 암반 히트 수 | 1 / 3 (곡괭이·드릴 기준, 폭탄은 항상 1) |
| 모래 압사 피해 | 초당 기름 5 |
| 보물 크기 / 표면 보장 깊이 | 3×3 / 3칸 이내 |
| 보물 보상 | 기름 +15 |
| 상점 벽 히트 수 | 20 |
| 상점 "보물 위치 표시" 가격 | 기름 8 |
| 안개 항상 밝은 표면 줄 수 | 2줄 |
| 상호작용 사거리 | 2m |

---

## 8. 이번 구현에서 제외된 것 (v1과 사유가 달라진 항목 포함)

| 항목 | 상태 | 사유 |
|---|---|---|
| 광물 (석탄/철/보석/큰보석) | 미구현 | 지형/기름 시스템을 먼저 검증하기 위해 의도적으로 비움. **다음 작업 1순위** |
| 가스 주머니 / 지하수 | 미구현 | 광물과 마찬가지로 "발견물" 체계 전체를 광물과 함께 넣을 예정 |
| 안개 2단계(내용물만 가림) / 인접 신호 / 표면 징후 / 줄기형 광맥 | 미구현 | 광물이 있어야 의미가 생기는 기능들이라 함께 미룸 |
| 턴제 조작 | 채택 안 함 | 기존 실시간 조작감을 유지하기로 결정 (2.2 참고) |
| 체력(PlayerHealth) | 삭제 | 기름 하나로 통합. 모래 압사도 기름 소모로 대체 |
| 재화 + 영구 업그레이드 페이즈 | 삭제 | 기름 통합 원칙과 상충 (도구가 항상 고정 비용이므로 업그레이드할 대상이 없음) |
| 유물(RelicManager, 무작위 버프) | 삭제 | 보물 보상을 기름 회복으로 단순화하며 대체 |
| 낙하 기름 소모 (v1 6장 "선택") | 미구현 | 실시간 물리 낙하로 대체되어 우선순위 낮음 |

---

## 9. 다음 단계 제안

1. **광물 추가** — 석탄/철/보석/큰보석을 층별로 배치하고 기름 회복량 부여. `GetStraightLineOilCost`가 v1이 의도한 "우회하면 항상 클리어 가능" 밸런스를 갖추려면 이 작업이 선행되어야 함
2. **안개 2단계 정교화** — 지형(흙/암반/모래 구분)은 미리 보여주고, 광물/가스 같은 "내용물"만 가리는 방식으로 전환 (v1 4.2절)
3. **줄기형 광맥 + 인접 신호** — 광물 배치를 무작위 점이 아니라 줄기 형태로, 주변 8칸 점 표시로 추론 가능하게
4. **위험 요소** — 가스 주머니(기름 -8, 3×3 파괴), 지하수(이동 비용 2배) 추가
5. **플레이테스트 체크리스트 재적용** (v1 9절) — 광물이 들어간 뒤 다시 10판 정도 돌려보고 "어디를 팔까 고민되는가" 등 5개 항목 재점검
